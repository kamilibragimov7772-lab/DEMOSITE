# Railway / Nixpacks конфиг для Astro static site.
# Сборка через `npm run build` (генерит dist/), затем static-server `serve` слушает $PORT.

[phases.setup]
nixPkgs = ["nodejs_20", "npm"]

[phases.install]
cmds = ["npm ci"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "npm start"
